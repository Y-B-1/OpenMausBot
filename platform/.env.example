# Copy to platform/.env and fill in. Existing shell env always wins.
# Anthropic direct — enables real Claude agents, Managed Agents, Agent SDK.
ANTHROPIC_API_KEY=
# Microsoft Foundry (optional) — Claude billed through Azure.
FOUNDRY_API_KEY=
FOUNDRY_RESOURCE=
# Generic OpenAI-compatible endpoint (optional) — Grok, DeepSeek, etc.
OPENAI_COMPAT_BASE_URL=
OPENAI_COMPAT_API_KEY=
